/**
 * APP_CONSTANTS centralizes application-wide constants to avoid magic values.
 */
export const APP_CONSTANTS = {
  API_PREFIX: 'api',
  API_VERSION: 'v1',
  DEFAULT_PORT: 3000 as const
} as const;
